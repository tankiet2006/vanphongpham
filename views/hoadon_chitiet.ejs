<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết hóa đơn</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --neon-green: #39FF14;
            --neon-aqua: #00FFFF;
            --neon-yellow: #FFEE32;
            --neon-red: #FF073A;
            --neon-orange: #ff8c00;
            --neon-purple: #A020F0;
            --dark-bg: #000;
            --card-bg: #111;
            --text-light: #EEE;
        }

        .text-aqua {
            color: var(--neon-aqua) !important;
            text-shadow: 0 0 8px var(--neon-aqua);
        }

        .border-neon {
            border-color: var(--neon-aqua) !important;
            opacity: 0.7;
        }

        .card-khachhang {
            background-color: var(--card-bg) !important;
            border: 1px solid var(--neon-green) !important;
            border-radius: 5px;
            box-shadow: 0 0 15px rgba(57, 255, 20, 0.2);
        }

        .card-sanpham {
            background-color: var(--card-bg) !important;
            border: 1px solid var(--neon-green) !important;
            border-radius: 5px;
            box-shadow: 0 0 15px rgba(57, 255, 20, 0.2);
        }

        .card-header {
            background: linear-gradient(90deg, #00FFFF, #39FF14);
            color: black;
            font-weight: bold;
        }

        .label-yellow {
            color: var(--neon-yellow) !important;
            font-weight: bold;
        }

        .text-purple {
            color: var(--neon-purple) !important;
        }


        .table {
            color: var(--neon-green);
            border-color: var(--neon-green) !important;
        }
        
        .table th {
            border: 1px solid var(--neon-green) !important;
            color: var(--neon-yellow) !important;
            background-color: var(--card-bg) !important;
            border-color: var(--neon-green) !important;
        }
        
        .table td {
            border: 1px solid var(--neon-green) !important;
            color: var(--neon-aqua) !important;
            background-color: var(--dark-bg) !important;
            border-color: var(--neon-green) !important;
        }

        .neon-orange-row td {
            background-color: #1a0e00;
            color: var(--neon-orange) !important;
        }

        .table tbody tr:hover td {
            background-color: var(--card-bg) !important;
            box-shadow: 0 0 5px var(--neon-aqua);
            transition: all 0.2s ease-in-out;
        }
    </style>
</head>
<body>
    <!-- INCLUDE HEADER -->
    <%- include('header') %>

    <!-- MAIN CONTENT -->
    <div class="container mt-3">
        <div class="row mb-2">
            <div class="col">
                <h2 class="text-aqua"><i class="bi bi-receipt"></i> Chi tiết hóa đơn</h2>
                <hr class="border-neon">
            </div>
        </div>

        <!-- Thông tin khách hàng -->
        <div class="card card-khachhang mb-4">
            <div class="card-header">Thông tin khách hàng</div>
            <div class="card-body">
                <p><span class="label-yellow">Ngày tạo:</span> <span class="text-purple"><%= hoaDon.ngayTao ? new Date(hoaDon.ngayTao).toLocaleDateString('vi-VN') : 'Không rõ' %></span></p>
                <p><span class="label-yellow">Họ tên:</span> <span class="text-purple"><%= hoaDon.HoVaTen %></span></p>
                <p><span class="label-yellow">Số điện thoại:</span> <span class="text-purple"><%= hoaDon.SoDienThoai %></span></p>
                <p><span class="label-yellow">Địa chỉ:</span> <span class="text-purple"><%= hoaDon.DiaChi %></span></p>
                <p><span class="label-yellow">Ghi chú:</span> <span class="text-purple"><%= hoaDon.GhiChu || 'Không có ghi chú' %></span></p>
            </div>

        </div>

        <!-- Bảng chi tiết sản phẩm -->
        <div class="card card-sanpham mb-4">
            <div class="card-header">Thông tin sản phẩm</div>
            <div class="card-body">               
                <table class="table table-bordered table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>#</th>
                            <th>Tên sản phẩm</th>
                            <th>Số lượng</th>
                            <th>Đơn giá</th>
                            <th>Thành tiền</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% let tongTien = 0; %>
                        <% hoaDon.danhSachSanPham.forEach((item, index) => { 
                            let ten = item.sanphamId?.TenSanPham || "Không tìm thấy";
                            let donGia = item.giaBan || 0;
                            let soLuong = item.soLuong;
                            let thanhTien = donGia * soLuong;
                            tongTien += thanhTien;
                        %>
                            <tr>
                                <td><%= index + 1 %></td>
                                <td><%= ten %></td>
                                <td><%= soLuong %></td>
                                <td><%= donGia.toLocaleString() %> đ</td>
                                <td><%= thanhTien.toLocaleString() %> đ</td>
                            </tr>
                        <% }) %>
                        <!-- Dòng tổng tiền -->
                        <tr class="neon-orange-row fw-bold">
                            <td colspan="4" class="text-end">Tổng tiền hóa đơn</td>
                            <td><%= tongTien.toLocaleString() %> đ</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- INCLUDE FOOTER -->
    <%- include('footer') %>
</body>
</html>