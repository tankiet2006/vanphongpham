<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cảm ơn</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" />
    <style>
        :root {
            --neon-pink: #FF00FF;
            --neon-aqua: #00FFFF;
            --neon-green: #39FF14;
            --neon-purple: #9D00FF;
            --neon-red: #FF073A;
        }
        
        body {
            background-color: var(--dark-bg);
            color: var(--text-light);
        }
        
        .thank-you-card {
            max-width: 900px;
            margin: 0 auto;
            border: 1px solid var(--neon-purple);
            box-shadow: 0 0 20px var(--neon-purple);
            background-color: var(--card-bg);
            position: relative;
            overflow: hidden;
        }

        .thank-you-header {
            background: rgba(26, 26, 46, 0.8);
            color: var(--neon-green);
            padding: 2.5rem 1rem;
            text-align: center;
            border-bottom: 1px solid var(--neon-purple);
            position: relative;
        }
        
        .thank-you-header h1 {
            font-weight: bold;
            text-shadow: var(--text-glow);
            letter-spacing: 2px;
        }
        
        .check-icon {
            color: var(--neon-green);
            text-shadow: 0 0 15px var(--neon-green);
            animation: pulse 1.5s infinite;
        }
        
        .order-id-badge {
            font-size: 1.1rem;
            background-color: var(--neon-purple) !important;
            color: #FFEE32 !important;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            border: 1px solid var(--neon-purple);
            text-shadow: var(--text-glow);
            display: inline-block;
        }
        
        .product-img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            border-radius: 5px;
            border: 1px solid var(--neon-green);
            box-shadow: 0 0 10px rgba(57, 255, 20, 0.3);
        }
        
        .summary-box {
            background-color: #1a1a2e; /* màu tối rõ hơn */
            border-radius: 8px;
            padding: 1.5rem;
            border: 2px solid var(--neon-pink); /* neon pink rõ ràng */
            box-shadow: 0 0 10px rgba(255, 0, 255, 0.5); /* tăng độ rõ */
        }

        .summary-label {
            color: #ccccff !important;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 1rem;
        }

        .summary-value {
            color: #ffffff !important;
            font-weight: 500;
            font-weight: bold;
            font-size: 1rem;
        }

        .total-row {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 2px solid var(--neon-purple); /* hoặc màu khác tùy bạn chọn */
        }

        .total-value {
            color: var(--neon-red) !important;
            font-weight: bold;
            font-size: 1.2rem;
            text-shadow: 0 0 10px rgba(255, 7, 58, 0.5);
        }

        .btnTiepTuc {
            background-color: var(--neon-green) !important;
            border: 1px solid var(--neon-green) !important;
            color: #000 !important;
        }
        
        .btnTiepTuc:hover {
            background-color: var(--neon-aqua) !important;
            border: 1px solid var(--neon-aqua) !important;
            box-shadow: 0 0 15px var(--neon-aqua) !important;
        }
        
        .alert-info {
            background-color: rgba(0, 255, 255, 0.1);
            border: 1px solid var(--neon-blue);
            color: var(--text-light);
        }
        
        .text-success {
            color: var(--neon-green) !important;
            text-shadow: var(--text-glow);
        }
        
        hr {
            border-color: var(--neon-purple);
            opacity: 0.5;
        }
        
        strong {
            color: var(--neon-blue);
        }
    </style>
</head>
<body>
    <!-- INCLUDE HEADER -->
    <%- include('header') %>
    
    <!-- MAIN CONTENT -->
     <div class="container py-5">
        <div class="thank-you-card rounded-3 overflow-hidden mb-5">
            <!-- Header -->
            <div class="thank-you-header text-center">
                <i class="bi bi-check-circle-fill display-3 mb-3 check-icon"></i>
                <h1 class="mb-0">ĐẶT HÀNG THÀNH CÔNG</h1>
            </div>
            
            <!-- Body -->
            <div class="thank-you-body p-4 p-md-5">
                <!-- Mã đơn hàng -->
                <div class="order-id text-center mb-5">
                    <span class="order-id-badge">
                        <i class="bi bi-receipt me-2"></i>
                        MÃ ĐƠN HÀNG: <strong><%= hoaDon._id %></strong>
                    </span>
                </div>
                
                <div class="row g-4">
                    <!-- Thông tin khách hàng -->
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-header bg-info text-dark fw-bold">
                                <i class="bi bi-person-lines-fill me-2"></i> THÔNG TIN KHÁCH HÀNG
                            </div>
                            <div class="card-body">
                                <ul class="list-unstyled">
                                    <li><strong>HỌ TÊN:</strong> <%= hoaDon.HoVaTen %></li>
                                    <li><strong>ĐIỆN THOẠI:</strong> <%= hoaDon.SoDienThoai %></li>
                                    <li><strong>EMAIL:</strong> <%= hoaDon.Email %></li>
                                    <li><strong>ĐỊA CHỈ:</strong> <%= hoaDon.DiaChi %></li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <!-- Sản phẩm đã đặt -->
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-header bg-info text-dark fw-bold">
                                <i class="bi bi-box-seam me-2"></i> SẢN PHẨM ĐÃ ĐẶT
                            </div>
                            <div class="card-body">
                                <% hoaDon.danhSachSanPham.forEach(sp => { %>
                                    <div class="d-flex align-items-center mb-3">
                                        <img src="<%= sp.sanphamId?.HinhAnh?.[0] || '/images/placeholder.jpg' %>" alt="" class="product-img me-3">
                                        <div>
                                            <h5 class="mb-1"><%= sp.sanphamId?.TenSanPham %></h5>
                                            <span>SL: <%= sp.soLuong %></span>
                                        </div>
                                    </div>
                                <% }) %>

                                <div class="summary-box mt-3">
                                    <div class="d-flex justify-content-between mb-2">
                                        <span class="summary-label">TẠM TÍNH:</span>
                                        <span class="summary-value"><%= (hoaDon.tongTien - 30000).toLocaleString() %>₫</span>
                                    </div>
                                    <div class="d-flex justify-content-between mb-2">
                                        <span class="summary-label">PHÍ VẬN CHUYỂN:</span>
                                        <span class="summary-value">30,000₫</span>
                                    </div>
                                    <div class="d-flex justify-content-between fw-bold fs-5">
                                        <span class="summary-label">TỔNG CỘNG:</span>
                                        <span class="total-value"><%= hoaDon.tongTien.toLocaleString() %>₫</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <% if (hoaDon.GhiChu) { %>
                    <div class="card mt-4">
                        <div class="card-header bg-warning text-dark fw-bold">
                            <i class="bi bi-chat-left-text me-2"></i> GHI CHÚ CỦA BẠN
                        </div>
                        <div class="card-body">
                            <%= hoaDon.GhiChu %>
                        </div>
                    </div>
                <% } %>

                <div class="alert alert-info mt-4 fw-bold text-center">
                    <i class="bi bi-info-circle-fill me-2"></i> CẢM ƠN BẠN ĐÃ ĐẶT HÀNG!
                </div>

                <div class="text-center mt-3 mb-4">
                    <a href="/sanpham" class="btnTiepTuc btn btn-lg px-5">
                        TIẾP TỤC MUA SẮM
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- INCLUDE FOOTER -->
     <%- include('footer') %>
</body>
</html>