<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tất cả sản phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --neon-green: #39FF14;
            --neon-aqua: #00FFFF;
            --neon-purple: #E100FF;
            --neon-yellow: #FFEE32;
            --neon-red: #FF073A;
            --dark-purple: #9c27b0;
            --dark-bg: #000;
            --card-bg: #111;
        }

        .btnSearch {
            background-color: var(--neon-green) !important;
            color: var(--dark-bg) !important;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .btnSearch {
            background-color: var(--neon-green) !important;
            color: var(--dark-bg) !important;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .btnSearch:hover {
            background-color: var(--neon-aqua) !important;
            box-shadow: 0 0 10px var(--neon-aqua);
            border: 1px solid var(--neon-aqua) !important;
        }

        .txtSearch {
            background-color: var(--dark-bg) !important;
            color: var(--neon-aqua) !important;
            border: 1px solid var(--neon-green) !important;
        }

        .txtSearch:focus {
            box-shadow: 0 0 10px var(--neon-aqua);
            border-color: var(--neon-aqua) !important;
        }

        input::placeholder {
            color: white !important;
            opacity: 1; /* Firefox */
        }

        .product-card {
            background-color: var(--card-bg) !important;
            border: 1px solid var(--dark-purple) !important;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }
        
        .product-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 20px var(--dark-purple);
            border-color: var(--dark-purple);
        }
        
        .product-img-container {
            padding: 10px;
            text-align: center;
            background: #000;
        }
        
        .product-img {
            height: 120px;
                width: 100%;
                object-fit: contain;
                border: 1px solid var(--neon-green);
            }
        
        .product-title {
            color: var(--neon-aqua) !important;
            font-weight: bold;
            margin-bottom: 10px;
        }
        
        .product-specs {
            margin-bottom: 10px;
        }
        
        .product-specs p {
            color: var(--neon-yellow) !important;
            margin-bottom: 5px;
            font-size: 0.8rem;
            line-height: 1.3;
        }   

        .product-specs strong {
            color: var(--dark-purple) !important; /* Purple color */
        }
        
        .product-price {
            font-size: 1rem;
            font-weight: bold;
            color: var(--neon-aqua) !important;
            margin: 10px 0;
        }
        
        .btnChiTiet {
            width: 100%;
            background-color: var(--neon-green) !important;
            color: var(--dark-bg) !important;
            border: var(--neon-green) !important;
            font-weight: bold !important;
            padding: 0.25rem 0.5rem;
            font-size: 0.8rem;
        }
        
        .btnChiTiet:hover {
            background-color: #FFEE32 !important;
        }

        /* Empty state */
        .alert-empty {
            background-color: rgba(57, 255, 20, 0.1) !important;
            border: 1px solid var(--neon-green);
            border-radius: 8px;
            text-align: center;
            padding: 30px;
        }

        .alert-empty i {
            font-size: 2rem;
            color: var(--neon-red) !important;
            margin-bottom: 15px;
        }

        .alert-empty h4 {
            color: var(--neon-red) !important;
            margin-bottom: 10px;
        }
        
        /* Section title */
        .text-aqua {
            color: var(--neon-aqua) !important;
            text-shadow: 0 0 8px var(--neon-aqua);
        }
        
        .border-neon {
            border-color: var(--neon-aqua) !important;
            opacity: 0.7;
        }
    </style>
</head>
<body>
    <!-- INCLUDE HEADER -->
    <%- include('header') %>

    <!-- INCLUDE NAVBAR -->
    <%- include('navbar') %>

    <!-- MAIN CONTENT -->
    <div class="container mt-4">
        <!-- DÒNG TIÊU ĐỀ + THANH TÌM KIẾM NGANG HÀNG -->
        <div class="d-flex flex-wrap justify-content-between align-items-center mb-1">
            <h2 class="text-aqua m-0">
                <i class="bi bi-stars"></i> Tất cả sản phẩm
            </h2>
            <form class="d-flex" role="search" style="max-width: 400px;">
                <input type="text" id="txtSearch" name="keyword"
                    class="txtSearch form-control me-2 search-input"
                    placeholder="Tìm kiếm tên sản phẩm...">
                <button type="button" onclick="searchProducts()"
                    class="btnSearch btn btn-success search-btn">
                    <i class="bi bi-search"></i>    
                </button>
            </form>
        </div>
        <hr class="border-neon">

        <!-- FILTER -->
        
        <!-- DANH SÁCH SẢN PHẨM -->
        <div class="row g-3">
            <% if (sanpham && sanpham.length > 0) { %>
                <% sanpham.forEach(sp => { %>
                    <div class="col-xl-2 col-lg-3 col-md-4 col-sm-6 col-6">
                        <div class="card h-100 product-card">
                            <div class="product-img-container" style="height: 150px; overflow: hidden;">
                                <img src="<%= (Array.isArray(sp.HinhAnh) && sp.HinhAnh.length > 0) ? sp.HinhAnh[0] : '/images/placeholder.jpg' %>" 
                                    alt="<%= sp.TenSanPham %>" 
                                    class="img-fluid w-100 h-100 object-fit-cover">
                            </div>

                            <div class="card-body p-2">
                                <h5 class="product-title fs-6 mb-2"><%= sp.TenSanPham %></h5>
                                <div class="product-specs small">
                                    <p><i class="bi bi-tag"></i> <strong>Hãng:</strong> <%= sp.HangSanXuat.TenHangSanXuat %></p>
                                    <p><i class="bi bi-percent"></i> <strong>Tỷ lệ:</strong> <%= sp.TyLe %></p>
                                    <p><i class="bi bi-palette"></i> <strong>Màu:</strong> <%= sp.MauSac %></p>
                                    <p><i class="bi bi-gem"></i> <strong>Chất liệu:</strong> <%= sp.ChatLieu %></p>
                                    <p><i class="bi bi-box-seam"></i> <strong>SL:</strong> <%= sp.SoLuong %></p>
                                </div>
                                <div class="product-price fs-6 mt-2">
                                    <span class="fw-bold"><%= sp.Gia.toLocaleString('vi-VN') %>₫</span>
                                </div>
                            </div>

                            <div class="card-footer p-2">
                                <a href="/sanpham/chitiet/<%= sp._id %>" class="btnChiTiet btn btn-outline-primary w-100 btn-sm">
                                    <i class="bi bi-info-circle"></i> Chi tiết
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12">
                    <div class="alert-empty text-center p-4">
                        <i class="bi bi-exclamation-triangle-fill fs-1 text-danger"></i>
                        <h4 class="text-danger mt-2">Hiện chưa có sản phẩm nào</h4>
                        <p class="text-success">Vui lòng quay lại sau...</p>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <!-- INCLUDE FOOTER -->
    <%- include('footer') %>
</body>
</html>