<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title><%= typeof title !== 'undefined' ? title : 'Lỗi' %> - Lỗi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body class="bg-dark text-light text-center">
    <div class="container mt-5">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h3>
                    <%= typeof title !== 'undefined' ? title : 'Lỗi' %>
                </h3>
            </div>
            <div class="card-body">
                <% if (typeof message !== "undefined" && message) { %>
                    <div class="alert alert-danger d-flex align-items-center px-3 py-2 rounded-3 border-0" role="alert">
                        <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
                        <div class="fw-semibold">
                            <%- message %>
                        </div>
                    </div>
                <% } else { %>
                    <p>Đã xảy ra lỗi không xác định.</p>
                <% } %>

                <% if (error && error.stack) { %>
                    <div class="alert alert-secondary text-start mt-3">
                        <pre><%= error.stack %></pre>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>