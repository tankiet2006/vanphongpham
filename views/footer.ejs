<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nova Cars - Cửa hàng mô hình xe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" />
    <style>
        :root {
            --neon-blue: #00f5ff;
            --neon-green: #39ff14;
            --neon-purple: #9c27b0;
            --bg-dark: #000;
        }

        body {
            background-color: var(--bg-dark);
        }

        .footer {
            background-color: var(--bg-dark);
            border-top: 1px solid var(--neon-blue) !important;
        }

        .footer .footer-title {
            color: var(--neon-blue);
            text-shadow: 0 0 6px var(--neon-blue);
            position: relative;
            display: inline-block;
            padding-bottom: 4px;
            font-weight: bold;
        }

        .footer .footer-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--neon-blue);
            box-shadow: 0 0 5px var(--neon-blue);
        }

        .footer .footer-text {
            color: var(--neon-green);
            text-shadow: 0 0 3px var(--neon-green);
        }

        .footer .social-icon {
            color: var(--neon-green);
            font-size: 1.5rem;
            margin: 0 10px;
            transition: transform 0.3s ease, color 0.3s;
        }

        .footer .social-icon:hover {
            transform: scale(1.2);
            color: var(--neon-purple);
            text-shadow: 0 0 8px var(--neon-purple);
        }

        .footer .brand-name {
            color: var(--neon-blue);
            text-shadow: 0 0 4px var(--neon-blue);
        }

        .footer .copyright {
            color: var(--neon-green);
            text-shadow: 0 0 3px var(--neon-green);
        }

        .footer .footer-column {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .footer p {
            margin-bottom: 5px;
        }

        .footer hr {
            border-color: var(--neon-green);
        }
    </style>
</head>

<body>
    <footer class="footer py-5 mt-5 border-top">
        <div class="container">
            <div class="row justify-content-center text-center">
                <div class="col-md-4 mb-4 footer-column">
                    <h5 class="footer-title"><i class="bi bi-info-circle"></i> Về chúng tôi</h5>
                    <p><span class="brand-name">Nova Cars</span> - <span class="footer-text">Đem đam mê đến mọi
                            nhà</span></p>
                </div>
                <div class="col-md-4 mb-4 footer-column">
                    <h5 class="footer-title"><i class="bi bi-headset"></i> Liên hệ</h5>
                    <p class="footer-text"><i class="bi bi-envelope"></i> Email: contact@novacars.com</p>
                    <p class="footer-text"><i class="bi bi-telephone"></i> Điện thoại: 0123.456.789</p>
                </div>
                <div class="col-md-4 mb-4 footer-column">
                    <h5 class="footer-title"><i class="bi bi-people"></i> Mạng xã hội</h5>
                    <div>
                        <a href="#" class="social-icon"><i class="bi bi-facebook"></i></a>
                        <a href="#" class="social-icon"><i class="bi bi-youtube"></i></a>
                        <a href="#" class="social-icon"><i class="bi bi-instagram"></i></a>
                        <a href="#" class="social-icon"><i class="bi bi-twitter-x"></i></a>
                    </div>
                </div>
            </div>
            <hr />
            <div class="text-center pt-3">
                <p class="copyright mb-0">
                    &copy; 2025 <span class="brand-name">Nova Cars</span>. All rights reserved.
                </p>
            </div>
        </div>
    </footer>
</body>

</html>