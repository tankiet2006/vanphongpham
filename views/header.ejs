<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nova Cars - Cửa hàng mô hình xe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --neon-green: #39FF14;
            --neon-aqua: #00FFFF;
            --neon-purple: #E100FF;
            --neon-red: #FF073A;
            --dark-bg: #000;
            --card-bg: #111;
        }

        body {
            background-color: var(--dark-bg);
        }

        .navHeader {
            color: var(--neon-aqua) !important;
            text-shadow: 0 0 10px var(--neon-aqua);
            text-decoration: none;
        }

        .border-bottom {
            border-color: var(--neon-green) !important;
        }

        .small {
            color: var(--neon-green);
        }

        .sdt-label {
            color: var(--neon-purple);
        }

        .email-label {
            color: var(--neon-red);
        }
    </style>
</head>

<body>
    <header class="header py-3 border-bottom">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h1 class="mb-0">
                        <a class="navHeader" href="/"><i class="bi bi-car-front-fill"></i> Nova Cars</a>
                    </h1>
                    <p class="mb-0 small">Nơi mua sắm mô hình xe uy tín</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="sdt-label d-inline-block me-3">
                        <i class="bi bi-headset"></i> Hỗ trợ: 0123.456.789
                    </div>
                    <div class="email-label d-inline-block">
                        <i class="bi bi-envelope"></i> contact@novacars.com
                    </div>
                </div>
            </div>
        </div>
    </header>
</body>

</html>