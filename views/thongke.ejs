<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Thống kê</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

    <style>
        body {
            background-color: #000;
            color: #eee;
        }

        .text-neon {
            color: #39FF14;
            text-shadow: 0 0 8px #39FF14;
        }

        .card {
            background-color: #111;
            border: 1px solid #39FF14;
            box-shadow: 0 0 10px #39FF14;
        }

        .card-header {
            background: linear-gradient(90deg, #00FFFF, #39FF14);
            color: black;
            font-weight: bold;
        }

        table {
            color: #eee;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h2 class="text-neon"><i class="bi bi-bar-chart-line"></i> Thống kê bán hàng</h2>
        <hr style="border-top: 2px solid #00FFFF;">

        <!-- Sản phẩm bán chạy nhất -->
        <div class="card mb-4">
            <div class="card-header">Sản phẩm bán chạy nhất</div>
            <div class="card-body">
                <% if (topSanPham) { %>
                    <p><strong>Tên sản phẩm:</strong>
                        <%= topSanPham.TenSanPham %>
                    </p>
                    <p><strong>Số lượng đã bán:</strong>
                        <%= topSanPham.soLuong %>
                    </p>
                    <% } else { %>
                        <p>Không có dữ liệu.</p>
                        <% } %>
            </div>
        </div>

        <!-- Doanh thu -->
        <div class="card">
            <div class="card-header">Thống kê doanh thu</div>
            <div class="card-body">
                <p><strong>Tuần này:</strong>
                    <%= doanhThu.tuan.toLocaleString() %> đ
                </p>
                <p><strong>Tháng này:</strong>
                    <%= doanhThu.thang.toLocaleString() %> đ
                </p>
                <p><strong>Quý này:</strong>
                    <%= doanhThu.quy.toLocaleString() %> đ
                </p>
                <p><strong>Năm nay:</strong>
                    <%= doanhThu.nam.toLocaleString() %> đ
                </p>
            </div>
        </div>
    </div>
</body>

</html>